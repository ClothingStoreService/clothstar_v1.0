<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.store.clothstar.order.repository.OrderRepository">
    <select id="getOrder" resultType="org.store.clothstar.order.domain.Order">
        select * from orders where order_id = #{orderId}
    </select>

    <insert id="saveOrder" parameterType="org.store.clothstar.order.domain.Order">
        INSERT INTO orders( order_id, member_id, address_id, total_shipping_price, created_at,
        status, total_products_price, payment_method, total_payment_price )
        VALUES( #{orderId}, #{memberId}, #{addressId}, #{totalShippingPrice}, #{createdAt},
        #{status}, #{totalProductsPrice}, #{paymentMethod}, #{totalPaymentPrice} )
    </insert>
</mapper>