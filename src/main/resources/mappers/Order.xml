<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.store.clothstar.order.repository.OrderRepository">
    <insert id="saveOrder" parameterType="org.store.clothstar.order.domain.Order">
        INSERT INTO orderline (order_id, member_id, delivery_id, created_dt, created_at, status, shipping_amt,
        products_amt,
        payment_method, payment_amt)
        VALUES (#{orderId}, #{memberId}, #{deliveryId}, #{createdDt}, #{createdAt}, #{status}, #{shippingAmt},
        #{productsAmt}, #{paymentMethod}, #{paymentAmt});
    </insert>

    <!--    <resultMap type="org.store.clothstar.order.domain.Order" id="order">-->
    <!--        <result property="orderId" column="order_id"/>-->
    <!--        <result property="memberId" column="member_id"/>-->
    <!--        <result property="deliveryId" column="delivery_id"/>-->
    <!--        <result property="createdDt" column="created_dt"/>-->
    <!--        <result property="createdAt" column="created_at"/>-->
    <!--        <result property="status" column="status"/>-->
    <!--        <result property="shippingAmt" column="shipping_amt"/>-->
    <!--        <result property="productsAmt" column="products_amt"/>-->
    <!--        <result property="paymentMethod" column="payment_method"/>-->
    <!--        <result property="paymentAmt" column="payment_amt"/>-->
    <!--    </resultMap>-->

    <!--    <select id="getOrder" resultMap="order">-->
    <!--        select * from orderline-->
    <!--        where order_id = #{orderId}-->
    <!--    </select>-->

    <select id="getOrder" resultType="org.store.clothstar.order.domain.Order">
        select * from orderline where order_id = #{orderId}
    </select>
</mapper>