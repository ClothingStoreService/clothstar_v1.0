<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.store.clothstar.member.repository.MemberRepository">
    <resultMap type="org.store.clothstar.member.domain.Member" id="member">
        <result property="memberId" column="member_id"/>
        <result property="sellerId" column="seller_id"/>
        <result property="telNo" column="tel_no"/>
        <result property="buyAmt" column="buy_amt"/>
        <result property="createdAt" column="created_at"/>
        <result property="modifiedAt" column="modified_at"/>
        <result property="deletedAt" column="deletedAt"/>
    </resultMap>

    <select id="findAll" resultMap="member">
        select * from member;
    </select>

    <select id="findById" resultMap="member">
        select * from member where member_id = #{memberId}
    </select>

    <select id="findByEmail" resultMap="member">
        select * from member where email = #{email}
    </select>

    <insert id="save" parameterType="org.store.clothstar.member.domain.Member">
        insert into member(email, password, name, tel_no)
        values(#{email}, #{password}, #{name}, #{telNo})
    </insert>
</mapper>